$schema: https://intentional.foo/schemas/config.yml
contract: contract-1
settings:
  internal-dependency-bump: patch
  pre-1-0-bump-mapping: compatibility
discovery:
  managed-paths:
  - detector: npm-package
    path: package.json
    release-unit: a2a-ahp
release-units:
  a2a-ahp:
    path: .
    projections:
    - adapter: npm
      file: package.json
      mode: committed
    tags:
      primary:
        role: primary
        template: '{version}'
